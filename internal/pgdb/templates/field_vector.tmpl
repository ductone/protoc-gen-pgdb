var {{.VarName}} interface{} = nullExp
for _, v := range m.self.{{.GoName}} {
    if v.{{.EnumName}} != {{.EnumModelValue}} {
        continue
    }
    {{.VarName}} = pgdb_v1.FloatArrayToVectorString(v.{{.FloatArrayName}})
}